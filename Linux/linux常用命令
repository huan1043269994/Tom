300个命令
大概200个常用
熟悉掌握60-70个


1.起始符
  root
  hw@hw-System-Product-Name:~$ 
  hw:用户
  hw-System-Product-Name:主机名
  ~ 当前所在位置，（家目录）初始登陆位置，初始根目录
    如果是用户模式下:/home/user1
    如果是管理员模式下:/root
  #:表示当前登陆的命令是管理员。
  
  
  2.命令格式：命令 [选项] [参数]
              个别命令不遵循此格式
              多个选项存在，在一起
              简化选项和完整选项
                  简化：-a
                  完整：--all 双杠
    1.pwd查看当前目录路径
    2.ls -h -l 参数（文件名等等，不加是操作所有，加了操作加的）
      -l:详细信息:-文件类型( - 文件 d 目录 | 软链接文件) 只有7种文件类型：下面3个+块设备+字符设备+套接字文件和管道文件  
      权限位10个位，：(drwxr-xr-x. 2 hw hw 4096 7月  15 23:33 Desktop)
          第一位: '-'代表普通文件
                  'd'代表目录
                  'l'软链接文件，快捷方式
          之后每三位为一组：所有者:rwxr，所属组:xr，其他人:x
                    所有者：u rw:所有权限， x r:执行+读
                    所属组：g xr:执行+读（用户加入了root权限）
                    其他人：o x:其他用户：只执行
                    用户组：一类人
                    点：'.' ACL权限
                      用户：单个人
                    数字：引用计数，被调用了几次
                    hw:代表所有者
                    hw（2）：所属组
                    4019:字节数
                    7月  15 23:33:最后一次修改时间
                    Desktop:文件名
        -h:人性化显示文件大小
        -a:显示所有文件，包含隐藏文件  .aaa开头的隐藏文件，基本是系统文件，不要乱动
        -d:查看目录属性
        -i:显示innode
