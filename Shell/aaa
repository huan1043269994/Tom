条件判断和流程控制语句
  test -e/root/install.log
  [ -e /root/install.log ]
  [ -d /root ] %% echo "yes" || echo "no" 判断文件是否存在
  
  -b 块
  -c 字符型
  -L 是否为符号链接文件
  -p 管道文件
  
  
  文件权限进行判断：
    -r:读
    -w:写
    -g:SUID
    -u:SGID
    -k:是否通配符
    -x:是否存在
  文件比较：
    -nt:新
    -ot:旧
    -ef:Inode是否一致，是不是同一个文件，一个文件和另外一个文件是不是硬链接
  整数比较：
    -eq
    -ne
    -gt
    -lt
    -ge
    -le
  字符串的比较：
    -e：判断是否为空
    -n：判断是否非空
    str1 == str2 是否相等
    str1 != str2 是否不想等
    判断两个数是否相等，==就可以了,aa=11,bb=22 aa,bb两个字符串相等，判断数字相等 -eq也可以
  多重条件判断
    -a:逻辑与
    -o:逻辑或
    !:逻辑非
  
