保存文件：相当于windows文件夹
文件：保存数据


1.
  mkdir -p [目录名]
    如果创建一级目录不要-p
    如果创建 japan/cangls 都不存在的多级目录要用-p
  rmdir [目录名]:只能删除空目录
  rm:
    -r:删除目录
    -f:强制删除
    -rf:现在强制删除任何东西都用rf了
  cd 
    cd ~
    cd  :回到家目录
    cd -:上次目录
    cd ..:上一级目录
    cd . :切换到当前目录
  相对路径：
  cd ../usr/local/src/
  如果上一级目录是根目录，那么可以执行，如果情况不是这样那么就不能只能
    如果在根目录下：都可以执行也就是(绝对路径)
    存在根目录下，一级一级递归执行，能进入指定位置
  tab:目录补全，命令补全
  pwd：打印目录路径
  cp:复制文件+目录
    -r：复制目录
    以上只复制文件本身不复制文件属性
    -p:文件属性
    -d:若源文件是链接文件，则复制链接属性
    -a:复制所有的东西
  mv:剪切 mv 源文件 目录
    剪切文件和目录一样格式
    mv 文件名 新文件名 ：相当于改名字(如果在同一个目录下）
    不在同一个目录下就是剪切
    
    
    
