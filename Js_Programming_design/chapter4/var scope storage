1.基本类型，引用类型
  js不能操作对象的内存空间，所以我们只能对引用操作。引用类型的值按引用访问的，也就是说能操作实际对象本身（例如添加属性）
  只能给引用类型的值动态的添加属性
  变量复制 存值按值传递
          存引用，复制值到变量，指向同一个对象。栈 堆
  但是传值：只能是副本。也就是把函数外部的值复制给函数内部。
  正则表达式是：对象，但是内部实现[[Call]]方法
  基本类型数据用：typeof
  引用类型数据用：instanceof
2.执行环境：定义了函数和变量有权访问其他的数据：都有与之关联的变量对象，我们编写代码无法访问到这个对象，但是后台会使用它。
  环境栈，执行流，作用域链：是保证对执行环境有权访问的所有变量和函数的有序访问
  活动对象：标志符解析
  js引擎要优化标志符查询。
  js具有自动垃圾收集机制
3.标记－垃圾收集，对于全局环境
4.周期性运行的垃圾收集器，如果垃圾很可观，回收工作量也是很大的
5.内存决定了在一个调用栈以及在一个线程中能够同时执行的语句数量， 开发人员学会管理内存：学会解除引用，适合大多数全局变量和全局对象的属性
6.占用固定大小空间的值保存在栈中，不固定的保存在堆中
