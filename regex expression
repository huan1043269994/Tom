1.正则表达式
  用于描述字符串的模式分割，匹配，查找，替换
  分割，查找，替换，匹配
  描述字符排列和匹配模式的一种语法规则
    完全匹配：
    通配符用来匹配（文件）名称，不能识别正则:find,cp,ls
    包含匹配：写得越长，匹配的就越精确
    正则表达式来匹配文件中的（数据/字符串）:grep,awk,sed
  基础正则：
  *:0个到多个
  .:任意"一个"
  ^:行首
  a$:行尾
  ^$:匹配空白行
  []
  [^]
  \
  \{n\}:前面的字母必须重复n次
  \{n,\}:至少n次
  ^[0-9]代表行首
  [^0-9]代表取反
2.字符截取命令
